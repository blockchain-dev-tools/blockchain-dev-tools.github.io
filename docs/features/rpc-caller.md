# RPC API 调用器 - 产品需求文档 (PRD)

**状态**: ✅ 核心功能已实现 | 🚧 增强功能开发中

## 1. 产品概述

### 1.1 产品定位
RPC API 调用器是一个用于直接调用多种区块链（EVM 兼容链、Solana、Tron、Sui 等）RPC 接口的开发者工具。该工具允许开发者通过友好的 Web 界面发送 JSON-RPC 请求，查看响应结果，并支持常用 RPC 方法的快速调用。

### 1.1.1 实现状态
- ✅ **核心功能**: 已完全实现并可用
- ✅ **多链支持**: 
  - EVM 链：6 条链（ETH, BNB, MATIC, Base, Arbitrum, Morph）
  - Solana：3 个网络（mainnet-beta, testnet, devnet）
  - Tron：3 个网络（mainnet, shasta, nile）
  - Sui：3 个网络（mainnet, testnet, devnet）
- ✅ **智能 RPC 选择**: 自动延迟测试和最佳端点选择
- ✅ **方法配置**: 
  - EVM：30+ 个 RPC 方法（包括 Debug API）
  - Solana：10+ 个 RPC 方法
  - Tron：15+ 个 RPC 方法
  - Sui：11+ 个 RPC 方法
- ✅ **Debug API 支持**: EVM 已添加 8 个 debug 方法
- ✅ **调用历史**: 支持删除单个和清空所有功能
- ✅ **cURL 导出**: 支持复制请求为 cURL 命令
- ✅ **英文界面**: 所有 UI 文本已改为英文
- ✅ **组件复用**: 通用 RPC Caller 组件，支持多链共享
- 🚧 **动态参数表单**: 计划后续增强
- 🚧 **批量调用**: 计划后续增强

### 1.2 目标用户
- 区块链开发者
- DApp 开发者
- 智能合约开发者
- 区块链测试人员
- 需要调试 RPC 调用的技术人员

### 1.3 核心价值
- **便捷性**：无需编写代码即可测试 RPC 调用
- **可视化**：友好的 UI 界面，清晰的请求/响应展示
- **高效性**：支持常用方法的快速调用模板
- **灵活性**：支持自定义 RPC 方法和参数
- **可追溯性**：保存调用历史，方便回顾和调试

## 2. 功能需求

### 2.1 核心功能

#### 2.1.1 RPC 端点配置 ✅ **已实现**
- **功能描述**：允许用户配置和切换不同的 RPC 端点，支持多链和智能 RPC 选择
- **详细需求**：
  - **支持的链** ✅：
    - Ethereum (ETH) - Chain ID: 1
    - Binance Smart Chain (BNB) - Chain ID: 56
    - Polygon (MATIC) - Chain ID: 137
    - Base - Chain ID: 8453
    - Arbitrum - Chain ID: 42161
    - Morph - Chain ID: 2819
  - **公共 RPC 端点** ✅：
    - 从 ChainList 获取每条链的公共 RPC 端点列表
    - 每条链提供多个备选 RPC 端点（3-5+ 个）
    - RPC 端点配置存储在独立的配置文件中（`src/config/rpc-endpoints.ts`）
  - **智能 RPC 选择** ✅：
    - 用户选择链后，自动测试所有可用的公共 RPC 端点
    - 测量每个 RPC 的延迟（ping 测试）
    - 自动选择延迟最短的 RPC 作为默认端点
    - 显示每个 RPC 的延迟信息（ms）
    - 支持手动切换 RPC 端点
    - 提供重新测试按钮刷新延迟测量
  - **自定义 RPC** ✅：
    - 支持用户手动输入自定义 RPC URL（HTTP/HTTPS）
    - 支持为每条链添加多个自定义 RPC
    - 自定义 RPC 保存到本地 localStorage
    - 自定义 RPC 也会参与延迟测试和自动选择
  - **端点管理** ✅：
    - 支持端点名称自定义和保存
    - 显示当前连接的端点状态
    - 支持手动刷新和重新测试所有端点

#### 2.1.2 RPC 方法调用 ✅ **已实现**
- **功能描述**：支持发送 JSON-RPC 请求并显示响应，提供详细的方法和参数说明
- **详细需求**：
  - **方法配置** ✅：
    - 所有支持的 RPC 方法配置存储在独立文件中（`src/config/rpc-methods.ts`）
    - 已配置 30+ 个 RPC 方法，包括：
      - 账户相关方法（4 个）
      - 区块相关方法（5 个）
      - 交易相关方法（5 个）
      - 合约相关方法（2 个）
      - 网络相关方法（4 个）
      - Debug 相关方法（8 个）
      - 其他方法（3 个）
    - 每个方法配置包括：
      - 方法名称（如 `eth_getBalance`）
      - 方法说明/描述（英文）
      - 方法分类（account, block, transaction, contract, network, debug, other）
      - 参数列表：
        - 参数名称
        - 参数数据类型（string, number, object, array, boolean, hex, address, blockTag 等）
        - 参数含义说明（英文）
        - 是否必需（required/optional）
        - 默认值（如果有）
        - 示例值
      - 对于对象类型参数，已详细描述对象内部字段：
        - 字段名称
        - 字段数据类型
        - 字段含义
        - 是否必需
        - 示例值
    - **eth_estimateGas 参数完善** ✅：
      - callObject 参数包含完整字段定义（from, to, gas, gasPrice, maxFeePerGas, maxPriorityFeePerGas, value, data）
      - 所有字段都有详细说明和示例
  - **方法选择** ✅：
    - 方法选择器使用下拉选择框，按分类分组展示
      - 账户相关（Account）
      - 区块相关（Block）
      - 交易相关（Transaction）
      - 合约相关（Contract）
      - 网络相关（Network）
      - Debug 相关（Debug）✅ 新增
      - 其他（Other）
    - 支持手动输入自定义方法名（不在配置中的方法）
    - 选择方法后显示方法说明和参数列表
  - **参数输入** ✅：
    - 支持 JSON 格式的参数输入（JSON 数组格式）
    - 参数验证（格式检查、必需参数检查）
    - 支持数组和对象类型的参数
    - 选择方法后自动填充默认参数值
  - **方法说明展示** ✅：
    - 选择方法后，在参数输入区域上方显示方法说明
    - 说明包括：方法描述、参数列表（参数名、类型、是否必需、说明）
  - **请求发送** ✅：
    - 发送按钮
    - 请求状态显示（发送中/成功/失败）
    - 请求超时设置（默认 30 秒）
    - 错误处理和提示

#### 2.1.3 响应展示 ✅ **已实现**
- **功能描述**：清晰展示 RPC 调用的响应结果
- **详细需求**：
  - **响应格式** ✅：
    - JSON 格式化显示（使用 pre 标签，可滚动查看）
    - 错误信息高亮显示（红色背景）
    - 响应结果区域可滚动（最大高度 400px）
  - **响应信息** ✅：
    - 显示响应时间（毫秒）- 在调用历史中显示
    - 错误信息高亮显示
  - **导出功能** ✅：
    - 复制响应到剪贴板
    - **复制为 cURL 命令** ✅ 新增功能
      - 成功响应时：显示 "Copy as cURL" 按钮
      - 错误响应时：显示 "Copy Request as cURL" 按钮
      - 生成的 cURL 命令可直接在终端执行
  - **数据解析** 🚧：
    - 计划支持：自动解析常见数据类型（地址、哈希、数值等）
    - 计划支持：十六进制到十进制的转换
    - 计划支持：Wei 到 Ether 的单位转换
    - 计划支持：时间戳转换（Unix 时间戳 → 可读时间）

#### 2.1.4 常用方法快速调用
- **功能描述**：为常用 RPC 方法提供快速调用模板
- **详细需求**：
  - **账户相关**：
    - `eth_getBalance`：查询账户余额
    - `eth_getTransactionCount`：查询 nonce
    - `eth_getCode`：查询合约代码
  - **区块相关**：
    - `eth_blockNumber`：获取最新区块号
    - `eth_getBlockByNumber`：根据区块号获取区块信息
    - `eth_getBlockByHash`：根据区块哈希获取区块信息
  - **交易相关**：
    - `eth_getTransactionByHash`：根据交易哈希获取交易信息
    - `eth_getTransactionReceipt`：获取交易回执
    - `eth_sendRawTransaction`：发送原始交易
  - **合约相关**：
    - `eth_call`：调用合约方法（只读）
    - `eth_estimateGas`：估算 Gas
  - **其他**：
    - `eth_gasPrice`：获取 Gas 价格
    - `eth_getLogs`：获取事件日志
    - `net_version`：获取网络 ID
    - `eth_chainId`：获取链 ID

### 2.2 辅助功能

#### 2.2.1 调用历史 ✅ **已实现**
- **功能描述**：保存和管理 RPC 调用历史
- **详细需求**：
  - 自动保存最近的调用记录（默认保存最近 50 条）✅
  - 显示调用时间、方法名、状态（成功/失败）✅
  - 显示请求耗时（毫秒）✅
  - 支持查看历史记录的详细信息 ✅
  - 支持重新执行历史调用（点击历史记录自动填充方法和参数）✅
  - **支持删除单个历史记录** ✅ 新增功能
    - 鼠标悬停时显示删除按钮（X 图标）
    - 点击删除按钮移除该记录
    - 自动更新 localStorage
  - **支持清空所有历史记录** ✅ 新增功能
    - 在 Call History 标题栏显示 "Clear All" 按钮（垃圾桶图标）
    - 点击时弹出确认对话框
    - 确认后清空所有记录并更新 localStorage
  - 支持导出历史记录（JSON 格式）🚧 计划中
  - 支持搜索历史记录（按方法名、时间等）🚧 计划中

#### 2.2.2 请求模板
- **功能描述**：提供常用场景的请求模板
- **详细需求**：
  - 模板分类（查询余额、发送交易、调用合约等）
  - 支持保存自定义模板
  - 支持模板的导入/导出
  - 模板参数可编辑

#### 2.2.3 批量调用
- **功能描述**：支持批量发送多个 RPC 请求
- **详细需求**：
  - 支持输入多个 JSON-RPC 请求（数组格式）
  - 批量发送并显示所有响应
  - 显示批量调用的统计信息（成功数、失败数、总耗时等）

#### 2.2.4 请求/响应导出 ✅ **部分实现**
- **功能描述**：支持导出请求和响应数据
- **详细需求**：
  - 导出为 JSON 文件 🚧 计划中
  - **导出为 cURL 命令** ✅ 已实现
    - 支持复制请求为 cURL 命令
    - 生成的 cURL 命令格式正确，可直接在终端执行
    - 正确处理 JSON 转义
  - 复制请求/响应到剪贴板 ✅
    - 复制响应 JSON
    - 复制请求为 cURL 命令
  - 支持导出为 Postman Collection 格式 🚧 计划中

### 2.3 用户体验功能

#### 2.3.1 界面布局
- **功能描述**：提供清晰、易用的界面布局
- **详细需求**：
  - 左侧：RPC 端点配置、方法选择、参数输入
  - 右侧：响应展示、调用历史
  - 支持响应式布局（移动端适配）
  - 支持暗色/亮色主题切换

#### 2.3.2 输入辅助
- **功能描述**：提供智能输入辅助功能
- **详细需求**：
  - 参数输入框支持 JSON 语法高亮
  - 参数输入框支持自动补全
  - 参数验证提示（实时验证）
  - 常用地址/哈希的快捷输入（最近使用的地址）

#### 2.3.3 错误处理
- **功能描述**：友好的错误提示和处理
- **详细需求**：
  - 网络错误提示（连接失败、超时等）
  - RPC 错误提示（错误码、错误信息）
  - 参数格式错误提示
  - 错误信息可复制

## 3. 技术实现方案

### 3.1 技术栈
- **前端框架**：Next.js 16 + React 19
- **UI 组件库**：shadcn/ui + Tailwind CSS
- **区块链库**：viem（用于类型定义和辅助功能）
- **HTTP 客户端**：原生 fetch API 或 axios
- **JSON 处理**：原生 JSON API
- **代码高亮**：react-syntax-highlighter 或 shiki
- **状态管理**：React Hooks (useState, useReducer)

### 3.2 架构设计

#### 3.2.1 组件结构 ✅
```
src/
├── app/
│   └── evm/
│       └── rpc-caller/
│           └── page.tsx                    # 页面入口 ✅
├── components/
│   └── features/
│       └── evm/
│           └── rpc-caller/                # RPC 调用器专用组件文件夹 ✅
│               ├── EvmRpcCaller.tsx        # 主组件 ✅（集成所有功能）
│               ├── RpcEndpointConfig.tsx   # RPC 端点配置组件 ✅
│               ├── types.ts                # 类型定义 ✅
│               └── utils.ts                # 工具函数 ✅
└── config/
    ├── rpc-endpoints.ts                    # RPC 端点配置文件 ✅
    └── rpc-methods.ts                      # RPC 方法配置文件 ✅
```

**实现说明**：
- ✅ 已实现的组件：主组件（集成方法选择、参数输入、响应展示、调用历史）、RPC 端点配置组件、类型定义、工具函数
- 🚧 计划拆分的组件：方法选择器、参数输入、响应展示、调用历史（当前集成在主组件中，后续可拆分为独立组件以提升可维护性）

#### 3.2.2 数据流
1. 用户选择/输入 RPC 端点
2. 用户选择/输入 RPC 方法
3. 用户输入参数（JSON 格式）
4. 点击发送按钮
5. 构建 JSON-RPC 请求
6. 发送 HTTP POST 请求到 RPC 端点
7. 接收响应并解析
8. 更新 UI 显示响应结果
9. 保存到调用历史

### 3.3 核心功能实现

#### 3.3.1 RPC 请求发送
```typescript
interface JsonRpcRequest {
  jsonrpc: '2.0';
  method: string;
  params: unknown[];
  id: number;
}

interface JsonRpcResponse {
  jsonrpc: '2.0';
  result?: unknown;
  error?: {
    code: number;
    message: string;
    data?: unknown;
  };
  id: number;
}

async function sendRpcRequest(
  endpoint: string,
  method: string,
  params: unknown[]
): Promise<JsonRpcResponse> {
  const request: JsonRpcRequest = {
    jsonrpc: '2.0',
    method,
    params,
    id: Date.now(),
  };

  const response = await fetch(endpoint, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify(request),
  });

  if (!response.ok) {
    throw new Error(`HTTP error! status: ${response.status}`);
  }

  return await response.json();
}
```

#### 3.3.2 常用方法模板
```typescript
const methodTemplates = {
  'eth_getBalance': {
    method: 'eth_getBalance',
    params: ['0x...', 'latest'],
    description: '查询账户余额',
  },
  'eth_blockNumber': {
    method: 'eth_blockNumber',
    params: [],
    description: '获取最新区块号',
  },
  // ... 更多模板
};
```

#### 3.3.3 响应数据格式化
- 使用 `viem` 库的格式化函数（如 `formatEther`, `formatUnits` 等）
- 自定义格式化函数处理地址、哈希等

### 3.4 配置文件设计

#### 3.4.1 RPC 端点配置文件 (`src/config/rpc-endpoints.ts`)
```typescript
export interface RpcEndpoint {
  url: string;
  name: string;
  chainId: number;
  isPublic: boolean; // 是否为公共端点
}

export interface ChainConfig {
  chainId: number;
  name: string;
  shortName: string; // eth, bnb, matic, base, arbitrum, morph
  publicEndpoints: RpcEndpoint[];
}

export const chainConfigs: ChainConfig[] = [
  {
    chainId: 1,
    name: 'Ethereum Mainnet',
    shortName: 'eth',
    publicEndpoints: [
      { url: 'https://eth.llamarpc.com', name: 'LlamaRPC', chainId: 1, isPublic: true },
      { url: 'https://rpc.ankr.com/eth', name: 'Ankr', chainId: 1, isPublic: true },
      { url: 'https://ethereum.publicnode.com', name: 'PublicNode', chainId: 1, isPublic: true },
      // ... 更多公共端点
    ],
  },
  {
    chainId: 56,
    name: 'BNB Smart Chain',
    shortName: 'bnb',
    publicEndpoints: [
      { url: 'https://bsc-dataseed.binance.org', name: 'Binance', chainId: 56, isPublic: true },
      { url: 'https://bsc-dataseed1.defibit.io', name: 'DefiBit', chainId: 56, isPublic: true },
      // ... 更多公共端点
    ],
  },
  // ... 其他链配置
];
```

#### 3.4.2 RPC 方法配置文件 (`src/config/rpc-methods.ts`)
```typescript
export type ParamType = 'string' | 'number' | 'boolean' | 'object' | 'array' | 'hex' | 'address' | 'blockNumber' | 'blockTag';

export interface ParamField {
  name: string;
  type: ParamType;
  description: string;
  required: boolean;
  default?: unknown;
  example?: unknown;
  fields?: ParamField[]; // 对象类型参数的嵌套字段
}

export interface RpcMethod {
  name: string;
  description: string;
  category: 'account' | 'block' | 'transaction' | 'contract' | 'network' | 'other';
  params: ParamField[];
  returns: {
    type: string;
    description: string;
  };
  example?: {
    request: unknown;
    response: unknown;
  };
}

export const rpcMethods: RpcMethod[] = [
  {
    name: 'eth_getBalance',
    description: '返回指定地址的账户余额（以 Wei 为单位）',
    category: 'account',
    params: [
      {
        name: 'address',
        type: 'address',
        description: '要查询余额的账户地址',
        required: true,
        example: '0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb',
      },
      {
        name: 'blockNumber',
        type: 'blockTag',
        description: '区块号或区块标签（latest, earliest, pending）',
        required: false,
        default: 'latest',
        example: 'latest',
      },
    ],
    returns: {
      type: 'string',
      description: '账户余额（十六进制字符串，单位为 Wei）',
    },
    example: {
      request: ['0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb', 'latest'],
      response: '0x1bc16d674ec80000',
    },
  },
  {
    name: 'eth_call',
    description: '执行一个消息调用，直接在节点的状态上执行，而不创建交易',
    category: 'contract',
    params: [
      {
        name: 'callObject',
        type: 'object',
        description: '调用对象',
        required: true,
        fields: [
          {
            name: 'to',
            type: 'address',
            description: '目标合约地址',
            required: true,
            example: '0x742d35Cc6634C0532925a3b844Bc9e7595f0bEb',
          },
          {
            name: 'data',
            type: 'hex',
            description: '要发送的数据（函数选择器和编码参数）',
            required: false,
            example: '0x...',
          },
          {
            name: 'from',
            type: 'address',
            description: '发送者地址',
            required: false,
            example: '0x...',
          },
          {
            name: 'gas',
            type: 'hex',
            description: 'Gas 限制',
            required: false,
          },
          {
            name: 'gasPrice',
            type: 'hex',
            description: 'Gas 价格',
            required: false,
          },
          {
            name: 'value',
            type: 'hex',
            description: '发送的 ETH 数量（Wei）',
            required: false,
          },
        ],
      },
      {
        name: 'blockNumber',
        type: 'blockTag',
        description: '区块号或区块标签',
        required: false,
        default: 'latest',
        example: 'latest',
      },
    ],
    returns: {
      type: 'string',
      description: '调用返回的数据（十六进制字符串）',
    },
  },
  // ... 更多方法配置
];
```

### 3.5 存储方案
- **调用历史**：使用 `localStorage` 存储（客户端存储）
- **自定义 RPC 端点**：使用 `localStorage` 存储用户自定义端点，格式：
  ```typescript
  {
    [chainId: number]: RpcEndpoint[] // 该链的自定义端点列表
  }
  ```
- **请求模板**：使用 `localStorage` 存储自定义模板
- **RPC 延迟缓存**：使用 `localStorage` 缓存 RPC 延迟测试结果（有效期 5 分钟）

### 3.6 RPC 延迟测试实现
```typescript
async function testRpcLatency(endpoint: string): Promise<number> {
  const startTime = Date.now();
  try {
    const response = await fetch(endpoint, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        jsonrpc: '2.0',
        method: 'eth_blockNumber',
        params: [],
        id: 1,
      }),
      signal: AbortSignal.timeout(5000), // 5秒超时
    });
    const endTime = Date.now();
    if (response.ok) {
      return endTime - startTime;
    }
    throw new Error('RPC request failed');
  } catch (error) {
    return Infinity; // 失败返回无穷大
  }
}

async function selectBestRpc(endpoints: RpcEndpoint[]): Promise<RpcEndpoint> {
  const latencyTests = await Promise.all(
    endpoints.map(async (endpoint) => ({
      endpoint,
      latency: await testRpcLatency(endpoint.url),
    }))
  );
  
  const validEndpoints = latencyTests.filter((test) => test.latency !== Infinity);
  if (validEndpoints.length === 0) {
    throw new Error('No available RPC endpoints');
  }
  
  // 选择延迟最短的
  const best = validEndpoints.reduce((prev, current) =>
    prev.latency < current.latency ? prev : current
  );
  
  return best.endpoint;
}
```

## 4. UI/UX 设计

### 4.1 页面布局

```
┌─────────────────────────────────────────────────────────┐
│  EVM RPC API 调用器                                      │
│  描述：直接调用 EVM 兼容链的 RPC 接口                    │
└─────────────────────────────────────────────────────────┘

┌──────────────────────┬──────────────────────────────────┐
│  配置区域             │  响应区域                        │
│                      │                                  │
│  [链选择]            │  ┌────────────────────────────┐ │
│  ┌────────────────┐  │  │  响应结果                  │ │
│  │ 链: [ETH ▼]   │  │  │  ┌──────────────────────┐ │ │
│  └────────────────┘  │  │  │ {                    │ │ │
│                      │  │  │   "jsonrpc": "2.0",  │ │ │
│  [RPC 端点选择]      │  │  │   "result": "...",   │ │ │
│  ┌────────────────┐  │  │  │   "id": 1            │ │ │
│  │ RPC: [自动选择]│  │  │  │ }                    │ │ │
│  │ 延迟: 120ms ✓ │  │  │  └──────────────────────┘ │ │
│  │ [切换] [添加]  │  │  │                            │ │
│  └────────────────┘  │  │  [复制] [导出] [格式化]    │ │
│                      │  └────────────────────────────┘ │
│  [方法选择]          │                                  │
│  ┌────────────────┐  │  [调用历史]                      │
│  │ [搜索方法...]  │  │  ┌────────────────────────────┐ │
│  └────────────────┘  │  │ 1. eth_getBalance          │ │
│                      │  │    2024-01-01 12:00:00    │ │
│  ┌────────────────┐  │  │    ✓ 成功                   │ │
│  │ 📁 账户相关    │  │  │  ────────────────────────── │ │
│  │   • eth_getBalance│ │  │ 2. eth_blockNumber         │ │
│  │   • eth_getTransactionCount│ │    2024-01-01 11:59:00    │ │
│  │   • eth_getCode  │  │  │    ✓ 成功                   │ │
│  │                  │  │  └────────────────────────────┘ │
│  │ 📁 区块相关    │  │                                  │
│  │   • eth_blockNumber│                                  │
│  │   • eth_getBlockByNumber│                              │
│  │   • eth_getBlockByHash│                               │
│  │                  │                                  │
│  │ 📁 交易相关    │  │                                  │
│  │ 📁 合约相关    │  │                                  │
│  │ 📁 网络相关    │  │                                  │
│  └────────────────┘  │                                  │
│                      │                                  │
│  [方法说明]          │                                  │
│  ┌────────────────┐  │                                  │
│  │ eth_getBalance │  │                                  │
│  │ 返回指定地址的账户余额（以 Wei 为单位）│              │
│  └────────────────┘  │                                  │
│                      │                                  │
│  [参数输入]          │                                  │
│  ┌────────────────┐  │                                  │
│  │ address*       │  │                                  │
│  │ [0x...]        │  │                                  │
│  │ 要查询余额的账户地址│                                  │
│  │                │  │                                  │
│  │ blockNumber    │  │                                  │
│  │ [latest]       │  │                                  │
│  │ 区块号或区块标签│  │                                  │
│  └────────────────┘  │                                  │
│                      │                                  │
│  [发送请求] [清除]   │                                  │
└──────────────────────┴──────────────────────────────────┘
```

### 4.1.1 方法选择器聚类排布设计

方法选择器采用**分组折叠面板**设计，按分类聚类展示：

```
┌─────────────────────────────────────┐
│  [🔍 搜索方法...]                    │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📁 账户相关 (Account)        [▼]   │
│  ├─ eth_getBalance                  │
│  ├─ eth_getTransactionCount         │
│  ├─ eth_getCode                     │
│  └─ eth_getStorageAt                 │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📁 区块相关 (Block)          [▼]   │
│  ├─ eth_blockNumber                 │
│  ├─ eth_getBlockByNumber             │
│  ├─ eth_getBlockByHash               │
│  └─ eth_getBlockTransactionCount     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📁 交易相关 (Transaction)    [▼]   │
│  ├─ eth_getTransactionByHash        │
│  ├─ eth_getTransactionReceipt       │
│  ├─ eth_sendRawTransaction          │
│  └─ eth_getTransactionByBlockHashAndIndex│
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📁 合约相关 (Contract)        [▼]   │
│  ├─ eth_call                         │
│  ├─ eth_estimateGas                  │
│  └─ eth_getCode                      │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📁 网络相关 (Network)        [▼]   │
│  ├─ net_version                      │
│  ├─ net_listening                   │
│  ├─ eth_chainId                      │
│  └─ eth_protocolVersion              │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📁 其他 (Other)              [▼]   │
│  ├─ eth_gasPrice                    │
│  ├─ eth_getLogs                     │
│  └─ eth_syncing                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  [手动输入方法名...]                  │
└─────────────────────────────────────┘
```

**交互特性**：
- 每个分类可以展开/折叠
- 支持搜索功能，搜索时高亮匹配的方法
- 点击方法后，自动填充到方法输入框
- 显示方法图标或分类图标，便于快速识别
- 支持键盘导航（上下箭头选择，回车确认）

### 4.2 交互流程

1. **首次使用**：
   - 用户打开页面
   - 默认选择 Ethereum 链
   - 自动测试所有可用的公共 RPC 端点
   - 自动选择延迟最短的 RPC 作为默认端点
   - 显示方法选择器（按分类聚类）和参数输入框
   - 显示快速模板

2. **切换链**：
   - 用户选择链（ETH/BNB/MATIC/Base/Arbitrum/Morph）
   - 系统自动测试该链的所有可用 RPC 端点
   - 自动选择延迟最短的 RPC
   - 显示每个 RPC 的延迟信息
   - 用户可手动切换或添加自定义 RPC

3. **选择方法**：
   - 用户在方法选择器中浏览（按分类聚类）
   - 或使用搜索功能查找方法
   - 选择方法后，显示方法说明
   - 动态生成参数输入表单
   - 每个参数都有独立的输入框和说明

4. **发送请求**：
   - 用户填写参数（或使用模板）
   - 系统验证参数（类型、格式、必需参数）
   - 点击"发送请求"按钮
   - 显示加载状态
   - 显示响应结果

3. **查看历史**：
   - 点击历史记录
   - 自动填充方法和参数
   - 可重新发送或查看详情

### 4.3 响应式设计
- **桌面端**：左右分栏布局
- **平板端**：上下布局，可折叠侧边栏
- **移动端**：单列布局，标签页切换

## 5. 安全考虑

### 5.1 数据安全
- **不存储敏感信息**：不在 localStorage 中存储私钥、助记词等敏感信息
- **HTTPS 要求**：生产环境强制使用 HTTPS
- **输入验证**：严格验证用户输入的 URL 和参数

### 5.2 网络安全
- **CORS 处理**：处理跨域请求（可能需要代理）
- **请求超时**：设置合理的请求超时时间
- **错误处理**：妥善处理网络错误，不暴露敏感信息

### 5.3 用户隐私
- **本地存储**：所有数据存储在用户本地浏览器
- **无后端**：不向服务器发送用户数据
- **可清除**：用户可随时清除所有存储的数据

## 6. 性能优化

### 6.1 前端优化
- **懒加载**：代码高亮库等大型依赖使用懒加载
- **防抖节流**：参数输入框使用防抖，避免频繁验证
- **虚拟滚动**：调用历史列表使用虚拟滚动（如果记录很多）

### 6.2 请求优化
- **请求取消**：支持取消正在进行的请求
- **并发控制**：批量请求时控制并发数量
- **缓存策略**：可选的响应缓存（如区块号等相对静态的数据）

## 7. 测试计划

### 7.1 功能测试
- RPC 端点配置和切换
- 各种 RPC 方法的调用
- 参数验证和错误处理
- 响应格式化和展示
- 调用历史的保存和检索

### 7.2 兼容性测试
- 不同浏览器的兼容性（Chrome、Firefox、Safari、Edge）
- 不同链的 RPC 端点兼容性
- 移动端浏览器测试

### 7.3 性能测试
- 大量历史记录的加载性能
- 复杂 JSON 的格式化性能
- 网络请求的响应时间

## 8. 开发计划

### 8.1 开发阶段

#### Phase 1: 核心功能（MVP）
- RPC 端点配置
- 基本方法调用
- 响应展示
- 常用方法模板（5-10 个）

#### Phase 2: 增强功能
- 调用历史
- 批量调用
- 数据格式化（Wei → Ether 等）
- 请求/响应导出

#### Phase 3: 优化和完善
- UI/UX 优化
- 性能优化
- 更多方法模板
- 错误处理完善

### 8.2 里程碑
- **Week 1-2**：完成 Phase 1（MVP）
- **Week 3-4**：完成 Phase 2（增强功能）
- **Week 5**：完成 Phase 3（优化和完善）
- **Week 6**：测试和修复

## 9. 后续扩展

### 9.1 功能扩展
- **WebSocket 支持**：支持 WebSocket RPC 连接
- **订阅功能**：支持 `eth_subscribe` 等订阅方法
- **交易构建器**：可视化构建交易参数
- **ABI 集成**：结合 ABI 解码器，自动解析合约调用
- **多链对比**：同时调用多个链的相同方法，对比结果

### 9.2 集成扩展
- **与交易解码器集成**：从 RPC 获取交易后直接解码
- **与 ABI 解码器集成**：调用合约方法后自动解码结果
- **导出到其他工具**：支持导出到 Postman、Insomnia 等

## 10. 成功指标

### 10.1 功能指标
- 支持至少 20 个常用 RPC 方法
- 支持至少 10 条主流 EVM 链
- 响应时间 < 3 秒（正常网络条件下）
- 错误处理覆盖率 100%

### 10.2 用户体验指标
- 界面加载时间 < 2 秒
- 操作响应时间 < 100ms
- 移动端适配良好
- 用户反馈评分 > 4.5/5

---

## 附录

### A. 已支持的 RPC 方法列表 ✅

#### 账户相关（4 个）
- ✅ `eth_getBalance(address, block)` - 查询账户余额
- ✅ `eth_getTransactionCount(address, block)` - 查询 nonce
- ✅ `eth_getCode(address, block)` - 查询合约代码
- ✅ `eth_getStorageAt(address, position, block)` - 查询存储位置的值

#### 区块相关（5 个）
- ✅ `eth_blockNumber()` - 获取最新区块号
- ✅ `eth_getBlockByNumber(blockNumber, fullTx)` - 根据区块号获取区块信息
- ✅ `eth_getBlockByHash(blockHash, fullTx)` - 根据区块哈希获取区块信息
- ✅ `eth_getBlockTransactionCountByNumber(blockNumber)` - 获取区块交易数量
- ✅ `eth_getBlockTransactionCountByHash(blockHash)` - 获取区块交易数量

#### 交易相关（5 个）
- ✅ `eth_getTransactionByHash(hash)` - 根据交易哈希获取交易信息
- ✅ `eth_getTransactionReceipt(hash)` - 获取交易回执
- ✅ `eth_getTransactionByBlockHashAndIndex(blockHash, index)` - 根据区块哈希和索引获取交易
- ✅ `eth_getTransactionByBlockNumberAndIndex(blockNumber, index)` - 根据区块号和索引获取交易
- ✅ `eth_sendRawTransaction(signedTx)` - 发送原始交易

#### 合约相关（2 个）
- ✅ `eth_call(callObject, block)` - 调用合约方法（只读）
- ✅ `eth_estimateGas(callObject, block)` - 估算 Gas（参数已完善）

#### 网络相关（4 个）
- ✅ `net_version()` - 获取网络 ID
- ✅ `net_listening()` - 检查是否正在监听
- ✅ `eth_chainId()` - 获取链 ID
- ✅ `eth_protocolVersion()` - 获取协议版本

#### Debug 相关（8 个）✅ 新增
- ✅ `debug_traceCall(callObject, blockNumber, traceOptions)` - 追踪调用
- ✅ `debug_traceTransaction(transactionHash, traceOptions)` - 追踪交易
- ✅ `debug_traceBlockByNumber(blockNumber, traceOptions)` - 追踪区块（按区块号）
- ✅ `debug_traceBlockByHash(blockHash, traceOptions)` - 追踪区块（按区块哈希）
- ✅ `debug_getRawHeader(blockNumber)` - 获取原始区块头
- ✅ `debug_getRawBlock(blockNumber)` - 获取原始区块
- ✅ `debug_getRawTransaction(transactionHash)` - 获取原始交易
- ✅ `debug_getRawReceipts(blockNumber)` - 获取原始收据

#### 其他（3 个）
- ✅ `eth_gasPrice()` - 获取 Gas 价格
- ✅ `eth_getLogs(filterObject)` - 获取事件日志
- ✅ `eth_syncing()` - 获取同步状态

### B. 支持的链和 RPC 端点

#### 支持的链
1. **Ethereum (ETH)** - Chain ID: 1
2. **BNB Smart Chain (BNB)** - Chain ID: 56
3. **Polygon (MATIC)** - Chain ID: 137
4. **Base** - Chain ID: 8453
5. **Arbitrum** - Chain ID: 42161
6. **Morph** - Chain ID: 2819

#### RPC 端点来源
- 所有公共 RPC 端点从 [ChainList](https://chainlist.org/) 获取
- 每条链提供多个备选公共端点
- 用户可添加自定义 RPC 端点
- 系统自动选择延迟最短的端点

#### 示例公共端点（需从 ChainList 获取完整列表）

**Ethereum Mainnet:**
- https://eth.llamarpc.com
- https://rpc.ankr.com/eth
- https://ethereum.publicnode.com
- https://eth.merkle.io

**BNB Smart Chain:**
- https://bsc-dataseed.binance.org
- https://bsc-dataseed1.defibit.io
- https://bsc-dataseed1.ninicoin.io

**Polygon:**
- https://polygon-rpc.com
- https://rpc.ankr.com/polygon
- https://polygon.llamarpc.com

**Base:**
- https://mainnet.base.org
- https://base.llamarpc.com
- https://base-rpc.publicnode.com

**Arbitrum:**
- https://arb1.arbitrum.io/rpc
- https://arbitrum.llamarpc.com
- https://arbitrum-one-rpc.publicnode.com

**Morph:**
- https://rpc.morphl2.io
- https://rpc-quicknode.morphl2.io

### C. 参考资源
- [JSON-RPC 2.0 规范](https://www.jsonrpc.org/specification)
- [Ethereum JSON-RPC API 文档](https://ethereum.org/en/developers/docs/apis/json-rpc/)
- [ChainList](https://chainlist.org/) - 公共 RPC 端点列表
- [viem 文档](https://viem.sh/)

### D. 配置文件位置 ✅

#### RPC 端点配置 ✅
- **文件路径**：`src/config/rpc-endpoints.ts`
- **内容**：所有支持的链及其公共 RPC 端点列表
- **更新方式**：定期从 ChainList 更新，或手动维护
- **状态**：已实现，支持 6 条链，每条链 3-5 个公共端点

#### RPC 方法配置 ✅
- **文件路径**：`src/config/rpc-methods.ts`
- **内容**：所有支持的 RPC 方法及其详细参数说明
- **更新方式**：根据 Ethereum JSON-RPC 规范维护
- **状态**：已实现，包含 30+ 个方法，包括 Debug API

#### 组件文件结构 ✅

**EVM RPC Caller 组件：**
- **主组件**：`src/components/features/evm/rpc-caller/EvmRpcCaller.tsx`
- **RPC 端点配置组件**：`src/components/features/evm/rpc-caller/RpcEndpointConfig.tsx`
  - **链选择器**：`src/components/features/evm/rpc-caller/ChainSelector.tsx`
  - **RPC 端点选择器**：`src/components/features/evm/rpc-caller/RpcEndpointSelector.tsx`
  - **自定义 RPC 列表**：`src/components/features/evm/rpc-caller/CustomRpcList.tsx`
  - **添加自定义 RPC 表单**：`src/components/features/evm/rpc-caller/AddCustomRpcForm.tsx`
- **类型定义**：`src/components/features/evm/rpc-caller/types.ts`
- **工具函数**：`src/components/features/evm/rpc-caller/utils.ts`
- **页面入口**：`src/app/evm/rpc-caller/page.tsx`

**通用 RPC Caller 组件（可复用）：**
- **位置**：`src/components/features/common/rpc-caller/`
- **方法选择器**：`MethodSelector.tsx`（支持 Tab 切换：选择方法 / 手动输入）
- **参数输入组件**：`ParameterInput.tsx`（动态生成表单字段）
- **响应展示组件**：`ResponseDisplay.tsx`（JSON 格式化、错误高亮、cURL 导出）
- **调用历史组件**：`CallHistory.tsx`（历史记录、重新执行、删除单个、清空所有）
- **类型定义**：`types.ts`（通用 RPC 相关类型）
- **工具函数**：`utils.ts`（延迟测试、RPC 请求、格式化、cURL 生成等）

**Solana RPC Caller 组件：**
- **主组件**：`src/components/features/solana/rpc-caller/SolanaRpcCaller.tsx`
- **RPC 端点配置组件**：`src/components/features/solana/rpc-caller/SolanaRpcEndpointConfig.tsx`
- **页面入口**：`src/app/sol/rpc-caller/page.tsx`
- **配置文件**：
  - `src/config/solana-rpc-methods.ts`（Solana RPC 方法配置）
  - `src/config/solana-rpc-endpoints.ts`（Solana 网络端点配置）

**Tron RPC Caller 组件：**
- **主组件**：`src/components/features/tron/rpc-caller/TronRpcCaller.tsx`
- **RPC 端点配置组件**：`src/components/features/tron/rpc-caller/TronRpcEndpointConfig.tsx`
- **页面入口**：`src/app/tron/rpc-caller/page.tsx`
- **配置文件**：
  - `src/config/tron-rpc-methods.ts`（Tron RPC 方法配置）
  - `src/config/tron-rpc-endpoints.ts`（Tron 网络端点配置）

**Sui RPC Caller 组件：**
- **主组件**：`src/components/features/sui/rpc-caller/SuiRpcCaller.tsx`
- **RPC 端点配置组件**：`src/components/features/sui/rpc-caller/SuiRpcEndpointConfig.tsx`
- **页面入口**：`src/app/sui/rpc-caller/page.tsx`
- **配置文件**：
  - `src/config/sui-rpc-methods.ts`（Sui RPC 方法配置）
  - `src/config/sui-rpc-endpoints.ts`（Sui 网络端点配置）

#### 用户自定义配置（localStorage）✅
- **键名**：`rpc-caller-custom-endpoints`
- **格式**：`{ [chainId: number]: RpcEndpoint[] }`
- **功能**：存储用户自定义的 RPC 端点
- **键名**：`rpc-caller-call-history`
- **格式**：`CallHistoryItem[]`
- **功能**：存储调用历史记录（最多 50 条）
- **键名**：`rpc-caller-rpc-latency-cache`
- **格式**：`{ [url: string]: { latency: number, timestamp: number } }`
- **功能**：缓存 RPC 延迟测试结果（5 分钟有效期）
- **键名**：`rpc-caller-selected-chain`
- **格式**：`string` (chain shortName)
- **功能**：保存用户选择的链
- **键名**：`rpc-caller-selected-endpoint`
- **格式**：`string` (endpoint URL)
- **功能**：保存用户选择的 RPC 端点

### E. 已实现功能总结 ✅

#### 核心功能
1. ✅ 多链支持（6 条链：ETH, BNB, MATIC, Base, Arbitrum, Morph）
2. ✅ 智能 RPC 选择（自动测试延迟并选择最佳端点）
3. ✅ 自定义 RPC 端点（添加、保存、参与延迟测试）
4. ✅ RPC 方法调用（30+ 个方法，包括 Debug API）
5. ✅ 方法配置（详细参数说明，支持对象类型嵌套字段）
6. ✅ 响应展示（JSON 格式化，错误高亮）
7. ✅ 调用历史（保存、查看、重新执行、删除单个、清空所有）
8. ✅ cURL 导出（复制请求为 cURL 命令）
9. ✅ 英文界面（所有 UI 文本使用英文）

#### 技术实现
- ✅ 配置文件分离（rpc-endpoints.ts, rpc-methods.ts）
- ✅ 组件模块化（独立的组件文件夹，已拆分为更小的子组件）
- ✅ 组件复用（通用 RPC Caller 组件，EVM 和 Solana 共享）
- ✅ 类型安全（完整的 TypeScript 类型定义）
- ✅ 本地存储（localStorage 持久化）
- ✅ 错误处理（友好的错误提示，Toast 通知系统）
- ✅ 响应式设计（支持桌面和移动端）
- ✅ UI 组件库（shadcn/ui：Select、Tabs、Toast、Button 等）
- ✅ 交互优化（手型光标、Tab 切换、条件显示等）

#### 组件重构记录 ✅
- ✅ **RpcEndpointConfig 重构**：拆分为 `ChainSelector`、`RpcEndpointSelector`、`CustomRpcList`、`AddCustomRpcForm` 四个独立组件
- ✅ **EvmRpcCaller 重构**：拆分为 `MethodSelector`、`ParameterInput`、`ResponseDisplay`、`CallHistory` 四个独立组件
- ✅ **通用组件提取**：将可复用的 RPC Caller 组件移至 `src/components/features/common/rpc-caller/`
- ✅ **Solana RPC Caller 实现**：基于通用组件实现 Solana 版本的 RPC Caller
- ✅ **Tron RPC Caller 实现**：基于通用组件实现 Tron 版本的 RPC Caller
- ✅ **Sui RPC Caller 实现**：基于通用组件实现 Sui 版本的 RPC Caller

#### 支持的区块链网络 ✅
- ✅ **EVM 兼容链**：Ethereum, BNB Smart Chain, Polygon, Base, Arbitrum, Morph
- ✅ **Solana**：Mainnet-beta, Testnet, Devnet
- ✅ **Tron**：Mainnet, Shasta Testnet, Nile Testnet
- ✅ **Sui**：Mainnet, Testnet, Devnet

